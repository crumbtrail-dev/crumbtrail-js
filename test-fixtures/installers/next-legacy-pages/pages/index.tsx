export default function Home() {
  return (
    <main>
      <h1>Installer fixture — Next.js Pages Router (legacy)</h1>
      <p>
        next &lt;15.3: client init is prepended into pages/_app so it actually
        runs in the browser.
      </p>
    </main>
  );
}
