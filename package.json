{
  "name": "crumbtrail-cli",
  "private": true,
  "description": "Crumbtrail SDKs and CLI — the published npm packages.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/CrumbtrailDev/crumbtrail-cli.git"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r --if-present run typecheck",
    "lint": "eslint .",
    "clean": "pnpm -r clean",
    "pack:local": "node scripts/pack-local.mjs",
    "verify:installers": "node scripts/verify-installers.mjs",
    "verify:fresh-install": "node scripts/verify-fresh-install.mjs",
    "verify:integration-docs": "pnpm --filter crumbtrail-node build && node scripts/verify-integration-docs.mjs",
    "refresh:installer-fixtures": "node scripts/refresh-installer-fixtures.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@vitest/coverage-v8": "^4.1.10",
    "eslint": "^10.6.0",
    "express": "^5.2.1",
    "globals": "^17.7.0",
    "happy-dom": "^17.4.0",
    "jsdom": "^26.1.0",
    "playwright": "^1.61.1",
    "prettier": "^3.5.0",
    "tsup": "^8.4.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.62.1",
    "vitest": "^4.1.10"
  }
}
